INCLUDE_DIRECTORIES(
 ${BULLET_PHYSICS_SOURCE_DIR}/Extras/VHACD/public
 ${BULLET_PHYSICS_SOURCE_DIR}/Extras/VHACD/inc
 ${BULLET_PHYSICS_SOURCE_DIR}/src
)

SET(VHACD_SRCS
	src/VHACD.cpp
	src/vhacdICHull.cpp
	src/vhacdManifoldMesh.cpp
	src/vhacdMesh.cpp
	src/vhacdVolume.cpp
)

SET(VHACD_HDRS
	public/VHACD.h
	inc/vhacdCircularList.h
	inc/vhacdCircularList.inl
	inc/vhacdICHull.h
	inc/vhacdManifoldMesh.h
	inc/vhacdMesh.h
	inc/vhacdMutex.h
	inc/vhacdTimer.h
	inc/vhacdVector.h
	inc/vhacdVector.inl
	inc/vhacdVHACD.h
	inc/vhacdVolume.h
)

ADD_LIBRARY(VHACD ${VHACD_SRCS} ${VHACD_HDRS})
SET_TARGET_PROPERTIES(VHACD PROPERTIES VERSION ${BULLET_VERSION})
SET_TARGET_PROPERTIES(VHACD PROPERTIES SOVERSION ${BULLET_VERSION})

#IF (BUILD_SHARED_LIBS)
#  TARGET_LINK_LIBRARIES(VHACD BulletCollision LinearMath)
#ENDIF (BUILD_SHARED_LIBS)

IF (INSTALL_EXTRA_LIBS)
	IF (NOT INTERNAL_CREATE_DISTRIBUTABLE_MSVC_PROJECTFILES)
		#FILES_MATCHING requires CMake 2.6
		IF (${CMAKE_MAJOR_VERSION}.${CMAKE_MINOR_VERSION} GREATER 2.5)
			IF (APPLE AND BUILD_SHARED_LIBS AND FRAMEWORK)
				INSTALL(TARGETS VHACD DESTINATION .)
			ELSE (APPLE AND BUILD_SHARED_LIBS AND FRAMEWORK)
				INSTALL(TARGETS VHACD DESTINATION lib${LIB_SUFFIX})
				INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
					DESTINATION ${INCLUDE_INSTALL_DIR} FILES_MATCHING PATTERN "*.h" PATTERN "*.inl" PATTERN
					".svn" EXCLUDE PATTERN "CMakeFiles" EXCLUDE)
			ENDIF (APPLE AND BUILD_SHARED_LIBS AND FRAMEWORK)
		ENDIF (${CMAKE_MAJOR_VERSION}.${CMAKE_MINOR_VERSION} GREATER 2.5)

		IF (APPLE AND BUILD_SHARED_LIBS AND FRAMEWORK)
			SET_TARGET_PROPERTIES(VHACD PROPERTIES FRAMEWORK true)
			SET_TARGET_PROPERTIES(VHACD PROPERTIES PUBLIC_HEADER "${VHACD_HDRS}")
		ENDIF (APPLE AND BUILD_SHARED_LIBS AND FRAMEWORK)
	ENDIF (NOT INTERNAL_CREATE_DISTRIBUTABLE_MSVC_PROJECTFILES)
ENDIF (INSTALL_EXTRA_LIBS)
